# {{ project_slug }} — documentation

Documentation is built with MkDocs Material. Run `make docs` and open <http://localhost:8000>.

API documentation is generated from docstrings in `src/{{ project_slug_underscore }}/`.
